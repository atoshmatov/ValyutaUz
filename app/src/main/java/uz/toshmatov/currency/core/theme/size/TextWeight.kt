package uz.toshmatov.currency.core.theme.size

import androidx.compose.ui.text.font.FontWeight

internal object TextWeight {
    val thin = FontWeight(400)
    val regular = FontWeight(500)
    val semiBold = FontWeight(600)
}
